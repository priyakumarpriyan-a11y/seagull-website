--- src/App.tsx
+++ src/App.tsx
@@ -611,10 +611,6 @@
 };
 
 // WorkflowSection
-const ProgressPercentage = ({ scrollYProgress }: { scrollYProgress: any }) => {
-  const [pct, setPct] = useState(0);
-  useMotionValueEvent(scrollYProgress, "change", v => setPct(Math.min(100, Math.round(v * 100))));
-  return <span>{pct}%</span>;
-};
-
 const WorkflowSection = () => {
   const steps = [
     {
